$C = "../../Common.ui";

$C.@PageOverlay {}

$C.@DecoratedContainer {
  Anchor: (Width: 760, Height: 680);

  #Title {
    Group {
      LayoutMode: Center;
      Anchor: (Full: 0);

      $C.@Title {
        @Text = %server.customUI.mermaid.title;
        Anchor: (Width: 200);
      }
    }
  }

  #Content {
    LayoutMode: Left;

    // Left sidebar - category list
    $C.@Panel #CategoryList {
      Anchor: (Width: 240);
      LayoutMode: TopScrolling;
      ScrollbarStyle: $C.@DefaultScrollbarStyle;
      Padding: (Top: 15, Bottom: 15, Left: 15, Right: 25);
    }

    Group {
      Anchor: (Width: 12);
    }

    // Main content area
    $C.@Panel #ContentArea {
      FlexWeight: 1;
      LayoutMode: TopScrolling;
      ScrollbarStyle: $C.@DefaultScrollbarStyle;
      Padding: (Top: 15, Bottom: 15, Left: 20, Right: 20);

      // Category title
      Group {
        LayoutMode: Left;

        Label #CategoryTitle {
          Style: (
            RenderUppercase: true,
            RenderBold: true,
            FontSize: 20,
            TextColor: $C.@ColorDefault
          );
          MaskTexturePath: $C.@TextHighlightGradientMask;
          Anchor: (Bottom: 15);
        }
      }

      // Dynamic content for each category
      Group #CategoryContent {
        LayoutMode: Top;
      }
    }
  }
}

$C.@BackButton {}
